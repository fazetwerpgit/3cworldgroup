'use client';

import { RepAsk } from '@/components/portal/rep/RepAsk';

// The chrome (top bar, tab bar, auth gate) comes from ./layout.tsx: RepShell.
export default function AskPage() {
  return <RepAsk />;
}
